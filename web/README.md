# KeepIn Web

Cliente Flutter do KeepIn focado em execução via navegador.

## Como rodar

1. Execute `flutter pub get` dentro de `web/`.
2. Garanta que o backend FastAPI esteja disponível.
3. Rode `flutter run -d chrome`.
